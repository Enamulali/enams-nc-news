import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ncnews.enam.com',
  appName: 'enams-nc-news',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
